Start
    INPUT price
    Set total = 0

    WHILE price >0
        total= total + price
        INPUT price

    IF total >= 100
       discount = total * 0.9
       TotalDiscount = total + discount
       OUTPUT TotalDiscount

    ELSE
        OUTPUT total

    ENDIF
END
